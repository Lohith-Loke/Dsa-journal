from ast import pattern
from opcode import haslocal
import re

class Solution:
    # @param A : string
    # @return an integer
    def solve(self, A):

        if len(A) > 7 and len(A) < 16:
            pattren = re.compile('[A-Z0-9a-a]')
            if pattern.:
                return 1
            return 0
        return 0


a = Solution()
x = a.solve("2Wl2S*4!i")
print(x)
